import React from 'react';

const Browser = (props) => {
    return (
        <div>
            <h1>Placeholder</h1>
            <button onClick={props.addToBasket}>Add to Basket</button>
        </div>
    );
};

export default Browser;